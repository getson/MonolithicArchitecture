namespace BinaryOrigin.SeedWork.Messages
{
    /// <summary>
    /// It is kind of message that notify that something has happened in the system.
    /// One event can have more than one handler
    /// </summary>
    public interface IEvent
    {
    }
}