version: '3.4'
services:
  postgres:
    image: postgres:12.2-alpine
  App-WebApi:
    image: app.api:${PLATFORM:-linux}-${TAG:-latest}
    build:
      context: .
      dockerfile: src/App.WebApi/Dockerfile
    depends_on:
      - postgres